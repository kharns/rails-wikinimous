
<ul class="list-unstyled">
  <%@articles.each do |article|%>
  <div>
    <li class="text-primary fs-2"><%= link_to article.title, article_path(article), class: "text-decoration-none" %></li>
    <li class="fs-5 fw-lighter"><%= article.content %></li>
  </div>
  <% end %>
</ul>
